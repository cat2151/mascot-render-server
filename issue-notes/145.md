# issue update サブコマンドで、cat-self-update-libを使うよう修正する #145
[issues #145](https://github.com/cat2151/mascot-render-server/issues/145)

- これまでの課題： 自前実装で不具合のある bat が使われ続けてしまっている
- 対策：update サブコマンドで、cat-self-updateリポジトリのcat-self-update-libを使うよう修正する
- 対策：実装方法は、cat-self-updateリポジトリを参考にせよ。clapクレートをもし使っていないなら使うよう修正せよ
- 対策：もしわからないことがあるなら失敗を報告せよ。「黙って失敗を握りつぶす」のは典型的なLLMの誤り、アンチパターンであり、固く禁止する
